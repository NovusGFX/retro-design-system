# Local-only working files not published to GitHub
# (canned redesigns, unaccepted styles, video pipeline)
_local-only/

# Claude Code per-developer permissions (machine-local, not for sharing)
.claude/settings.local.json

# OS / editor cruft
.DS_Store
Thumbs.db
